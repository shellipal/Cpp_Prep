#include <iostream> //#include is a preproccessor derective...which is used to includes files;
// iostream are those files which we includes in our programm for (cin -> for take inputs from user) , (cout -> for displaying an output) , etc...........

using namespace std; // this brings standard names into scopes...No needs to add prefix -> std::

int main()
{
    cout << "Hello Shelli" << endl;
    return 0;
}